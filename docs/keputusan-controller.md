# Keputusan yang diambil controller selama eksekusi

Setiap keputusan di bawah diambil tanpa menunggu jawaban Anda, agar eksekusi rencana
tidak berhenti. Formatnya: apa yang diputuskan, mengapa, dan apa biayanya bila keliru.
Silakan tinjau; yang menurut Anda salah dapat dibalik.

Jumlah keputusan: 38. Nomor 1 sampai 17 diambil pada sesi 14 September 2026,
sisanya pada sesi 15 September 2026.

## 1. T15 hanya menyisipkan `page-pengaturan.js` ke ENAM halaman yang disebut di blok Files-nya, bukan ke `pages/layout.html`. — Blok Files menyebut enam berkas secara eksplisit, sedangkan Step 2 menulis "setiap berkas di pages/"; `layout.html` adalah template rujukan, bukan halaman yang dipakai pengguna, sehingga tidak perlu modal pengaturan. — Bila keliru: `pages/layout.html` tidak punya modal Pengaturan; menu Pengaturan di template itu tidak melakukan apa-apa saat diklik. Biaya perbaikan satu baris.

## 2. ID model yang dipakai adalah `gpt-5.6-sol` dan `gpt-6-astra`, bukan varian `-codex-`. — Pengujian langsung terhadap empat kandidat: `gpt-5.6-codex-sol` dan `gpt-6-codex-astra` ditolak ("Model metadata not found, defaulting to fallback"), sedangkan `gpt-5.6-sol` dan `gpt-6-astra` berjalan normal; `gpt-6-astra` juga muncul di `~/.codex/config.toml`. — Bila keliru: dispatch gagal cepat dengan galat model dan saya kehilangan satu putaran per task, bukan kerusakan kode.

## 3. controller (saya) yang menjalankan `git commit` untuk setiap task, bukan Codex. — Sandbox Codex menolak tulis ke `.git` dan eskalasi dimatikan, sehingga instruksi commit di rencana tidak dapat dijalankan implementer. Commit adalah pembukuan mekanis, bukan perbaikan temuan, sehingga tidak melanggar aturan "jangan memperbaiki sendiri di sesi controller". — Bila keliru: riwayat git tetap benar isinya, tetapi penulis commit adalah saya, bukan implementer; tidak ada dampak pada kode.

## 4. perbaiki `geser()` di `data.js` agar membentuk string ISO dari komponen tanggal LOKAL, bukan lewat `toISOString()`. — `toISOString()` mengonversi ke UTC lebih dulu, sehingga tengah malam lokal di zona yang lebih maju dari UTC masih terhitung hari sebelumnya; seluruh zona waktu Indonesia (UTC+7/8/9) terkena. Spec Bagian 6 mengikat bahwa status "jatuh tempo hari ini" dan "terlambat" harus relevan kapan pun didemokan, dan kode rencana gagal memenuhi maksud spec itu — spec adalah otoritas yang mengikat, rencana hanyalah argumennya. — Bila keliru: tanggal seed bergeser sehari ke arah sebaliknya; terdeteksi langsung oleh pemeriksaan `cocok: true` di putaran perbaikan.

## 5. koreksi yang sama dibawa ke Task 4 untuk `Store.rules.hariIni()`, yang di rencana memakai pola `toISOString()` identik. — Bug yang sama akan lolos ke lapisan aturan bisnis dan membuat perhitungan denda serta status keterlambatan meleset sehari terhadap kalender nyata. Ini temuan load-bearing: task berikutnya dibangun di atasnya. — Bila keliru: `hariIni()` memakai komponen lokal sementara sisa aritmetika memakai UTC; sudah diperiksa bahwa `tambahHari`/`selisihHari` memakai `Date.UTC` atas string ISO murni sehingga tidak terpengaruh zona.

## 6. `db.urutanId` yang ditambahkan Codex DIPERTAHANKAN, bukan dikembalikan sebagai scope creep. — Reviewer menelusuri dan membuktikan bahwa kode `idBerikutnya` di brief BERTENTANGAN dengan komentarnya sendiri: komentar menjanjikan ID tidak boleh terpakai ulang setelah penghapusan, tetapi kode yang memindai nilai tertinggi pada koleksi saat ini akan menerbitkan ulang ID baris yang baru dihapus. Versi Codex memenuhi janji itu, sudah diuji regresi secara khusus, tidak bertabrakan dengan kontrak seed Task 2, dan grep atas seluruh brief Task 4-17 tidak menemukan satu pun yang membaca bentuk mentah database. — Bila keliru: satu field tambahan di payload localStorage yang tidak dibaca siapa pun; dampaknya nol pada perilaku aplikasi.

## 7. temuan Important "all()/find() mengembalikan referensi hidup" DIPERBAIKI, tidak diparkir. — Ini batas lapisan data yang justru menjadi inti Task 3, dan Task 9-14 adalah lapisan UI yang dibangun persis di atasnya; sekali sebuah halaman memutasi baris hasil `find()`, perubahan liar itu ikut terserialisasi ke `elibrary.db.v1` pada penulisan berikutnya dan sangat sulit dilacak. Biaya perbaikan dua baris, biaya membiarkannya adalah kelas bug yang tak terlihat sampai data pengguna rusak. — Bila keliru: ada penyalinan dangkal per baris pada setiap pembacaan; pada volume data ini (24 buku, 18 anggota) dampaknya tidak terukur.

## 8. dua observasi baru dari re-reviewer (`create()`/`update()` mengembalikan referensi hidup) DITUNDA ke review akhir, tidak dimasukkan ke fix loop. — Aturan skill tegas bahwa observasi di luar cakupan masuk ledger sebagai minor tertunda dan tidak pernah memperpanjang putaran; memperluas loop setiap kali reviewer melihat isu bertetangga adalah cara loop tidak pernah konvergen. Saya periksa juga bahwa tidak ada konsumen terencana di Task 9-14 yang memutasi nilai kembalian `create()` atau `update()`, jadi penundaan ini aman. — Bila keliru: sebuah halaman memutasi hasil `create()` dan mengubah store tanpa lewat `update()`; terdeteksi di review akhir menyeluruh yang secara eksplisit saya arahkan ke butir ini.

## 9. tambahkan penjaga idempoten di `prosesPengembalian()` alih-alih mengandalkan setiap halaman UI mengingat memeriksanya. — Pemanggilan kedua menambah stok lagi (di atas batas `jumlahTotal` saja) dan membuat baris denda duplikat tanpa dedup; Task 12 membangun UI pengembalian sebagai modal dengan tombol konfirmasi, sehingga klik ganda atau konfirmasi yang terkirim ulang akan merusak stok dan catatan denda tanpa pesan galat apa pun. Menaruh penjaga di lapisan data melindungi semua pemanggil sekaligus, bukan bergantung pada disiplin tiap halaman. — Bila keliru: alur sah yang ingin memproses ulang pengembalian (mis. mengoreksi tanggal kembali) akan ditolak; tidak ada alur seperti itu di rencana, dan koreksi semacam itu memang seharusnya lewat `update()`.

## 10. fix round 1 Task 4 diperluas sebelum re-review, bukan dijadikan round 2 terpisah. — Satu putaran perbaikan adalah satu dispatch perbaikan ditambah satu re-review terarah; saya menemukan perbaikannya belum lengkap SEBELUM re-review dijalankan, sehingga melengkapinya dalam dispatch yang sama lebih tepat daripada meminta re-review memverdikt perbaikan yang sudah saya tahu setengah jadi. — Bila keliru: satu putaran terhitung lebih panjang dari seharusnya di ledger; tidak ada dampak pada kode.

## 11. celah focus trap DIPERBAIKI sekarang, tidak ditunda. — Task 15 membuka modal Pengaturan yang persis berbentuk "hanya tombol Batal", jadi celahnya pasti terpicu, bukan hipotetis; dan Task 17 Step 8 sudah mensyaratkan pemeriksaan navigasi keyboard, sehingga menundanya hanya memindahkan pekerjaan yang sama ke titik ketika tujuh halaman sudah dibangun di atas modal yang sama. — Bila keliru: sekitar 20 baris penanganan Tab yang tidak terpakai; tidak ada risiko regresi karena jalur pembersihan listener tidak diubah.

## 12. fixture tes urut angka diperbaiki, bukan ditunda. — Ini kelas cacat yang sama dengan yang saya temukan di Task 4 (tes lolos dengan maupun tanpa perbaikan), dan `urutkan` dipakai empat halaman untuk kolom stok, jumlah pinjaman, sisa hari, dan nominal denda — semuanya nilai yang akan melewati 9 pada data nyata, sehingga regresi ke pengurutan teks akan menampilkan urutan yang salah tanpa galat apa pun. — Bila keliru: satu baris fixture berubah; tidak ada risiko regresi karena kode produksi tidak disentuh.

## 13. penjaga sesi diperketat agar hanya menerima objek dengan `nama` bertipe string, bukan sekadar nilai truthy. — Reviewer menilai risikonya rendah karena hanya terpicu oleh penyuntingan localStorage manual, tetapi saya menemukan konsekuensi konkret di rencana: Task 12 menulis `idPetugas: Layout.sesi().id` saat mencatat peminjaman, sehingga sesi yang tidak berbentuk objek petugas akan diam-diam menghasilkan transaksi dengan `idPetugas: undefined` — data rusak tanpa galat, persis pola yang sudah empat kali muncul di proyek ini. Penjaga yang meloloskan non-sesi juga melanggar kontraknya sendiri yang tertulis "objek petugas atau null". — Bila keliru: sesi lama yang bentuknya berbeda akan dipaksa login ulang sekali; tidak ada kehilangan data karena sesi hanya menyimpan identitas petugas.

## 14. temuan Important DIPERBAIKI dengan menambah diskriminator field pada nilai kembalian `bolehPinjam`, bukan dengan mencocokkan isi pesan. — `bolehPinjam` punya lima cabang penolakan, dua di antaranya soal BUKU ("Buku tidak ditemukan", "Stok X sedang habis"), tetapi halaman selalu menempelkan alasannya ke field Anggota. Pustakawan yang melihat "Stok habis" di bawah label Anggota akan memeriksa kontrol yang salah. Mencocokkan isi pesan untuk menebak field-nya rapuh terhadap perubahan kata; menambah `field: 'anggota' | 'buku'` bersifat aditif sehingga konsumen lama dan seluruh tes yang memeriksa `.boleh`/`.alasan` tidak terpengaruh. — Bila keliru: satu properti tambahan pada objek kembalian yang tidak dibaca siapa pun; nol risiko pada perilaku.

## 15. `bukuDariPinjam()` dijadikan accessor publik yang mengembalikan salinan, sementara `prosesPengembalian()` mencari baris hidup sendiri lewat `pastikan()`. — Setelah perbaikan Task 3, kode brief `prosesPengembalian` akan memutasi salinan sehingga pemulihan stok hilang tanpa jejak; ini bug diam yang baru terlihat saat stok tidak pernah bertambah setelah pengembalian. Memisahkan jalur publik dari jalur internal mempertahankan kedua jaminan sekaligus. — Bila keliru: `bukuDariPinjam` mengembalikan referensi hidup ke halaman UI, membuka kembali kelas bug yang baru ditutup di Task 3.

## 16. reviewer task N boleh berjalan bersamaan dengan implementer task N+1 selama N+1 membuat berkas yang tidak beririsan dan hanya mengonsumsi antarmuka publik N. — Skill melarang beberapa IMPLEMENTER berjalan paralel (risiko konflik berkas), bukan implementer bersama reviewer; Task 3 hanya membuat `store.js` dan `tests/*` sementara Task 2 hanya menyentuh `data.js`, dan tes Task 3 justru akan langsung menyingkap bila data Task 2 salah. Serialisasi penuh atas 17 task akan menggandakan waktu tanpa menambah keamanan. — Bila keliru: temuan review Task N memerlukan perbaikan `data.js` sementara Task N+1 sudah dibangun di atasnya; karena berkasnya terpisah, biayanya hanya satu putaran perbaikan tambahan, bukan penulisan ulang.

## 17. verifikasi peramban (render glass, toggle tema, console bersih) dipindahkan dari implementer ke controller. — Codex tidak dapat menjalankan peramban; laporannya menyatakan hal itu secara jujur dan hanya memverifikasi sintaks JS, ketertutupan tag HTML, dan respons HTTP. Membiarkan verifikasi visual tidak dilakukan sama sekali akan membuat klaim "selesai" tidak berdasar. — Bila keliru: cacat visual baru ketahuan di Task 17 dan biaya perbaikannya lebih besar karena sudah ditumpuk halaman lain.

## 18. harness diperbarui mengikuti perbaikan, BUKAN perbaikan dilemahkan agar harness lolos. — Temuan review Task 12 sudah menetapkan bahwa alasan penolakan buku milik field Buku; sebuah tes yang menuntut sebaliknya adalah tes yang salah, dan dua asersi tetangganya ("Diblokir", "batas maksimal") memang benar tetap di idAnggota sehingga hanya satu asersi yang perlu pindah. Asersi penggantinya harus DUA arah — pesan /habis/ ADA di bawah idBuku DAN TIDAK ADA di bawah idAnggota — agar tes benar-benar membedakan, sesuai standar yang sudah dipakai proyek ini sejak commit 7a642f0 dan 2674d71. — Bila keliru: satu asersi tes menandai field yang salah; nol dampak pada kode produksi, ketahuan di review berikutnya.

## 19. Task 14 TIDAK dijalankan paralel dengan review Task 13, meskipun ruling sesi sebelumnya mengizinkan reviewer task N berjalan bersama implementer task N+1. — Syarat ruling itu adalah berkas yang tidak beririsan; di sini syaratnya tidak terpenuhi, karena perbaikan Task 13 dan Task 14 sama-sama menulis ke `assets/css/style.css`, dan pre-flight sudah menetapkan blok `@media print` milik Task 14 harus menjadi yang PALING AKHIR di berkas itu. Menjalankan keduanya bersamaan berisiko menyisipkan CSS Task 13 setelah blok cetak. — Bila keliru: saya kehilangan sedikit paralelisme pada satu task; nol risiko pada kode.

## 20. reviewer TIDAK lagi diminta menulis berkas laporan; mereka mengembalikan laporan langsung sebagai balasan. — Sandbox Codex bersifat read-only untuk agen review, sehingga permintaan menulis `rereview-task-12.md` gagal dan laporan tetap harus dikirim sebagai teks balasan. Menuntut penulisan berkas hanya menambah satu langkah yang pasti gagal. — Bila keliru: laporan review tidak tersimpan sebagai artefak terpisah; isinya tetap terekam di ledger ini lewat ringkasan temuan, dan diff-nya tetap ada di paket review.

## 21. temuan Important diperbaiki dengan membuat `tulis()` mengembalikan boolean, lalu halaman memeriksa keberhasilan lewat `Store.simpan()` yang SUDAH ADA di API publik — bukan dengan mengubah kontrak kembalian `create()`/`update()`. — Mengubah `create`/`update` agar mengembalikan null saat gagal akan menyentuh kontrak yang dipakai Task 3, 4, 10, 11, dan 12 sekaligus, memaksa verifikasi ulang lima task yang sudah bersih, padahal perbaikan yang dibutuhkan hanya di satu halaman. Membuat `tulis()` mengembalikan boolean bersifat aditif karena saat ini nilai kembaliannya tidak dibaca siapa pun, dan `Store.simpan` sudah diekspos sehingga tidak ada permukaan API baru yang ditambahkan. — Bila keliru: satu pemanggilan tulis tambahan per penyimpanan (probe), yang biayanya tidak terasa pada data sebesar ini, dan halaman lain tetap berperilaku persis seperti sekarang.

## 22. pembatalan mutasi di memori TIDAK diwajibkan pada perbaikan ini. — Kontrak yang penting bagi pengguna adalah tidak diberi tahu "tersimpan" untuk sesuatu yang gagal dan tidak dilempar keluar dari form yang berisi ketikannya; karena halaman TIDAK berpindah saat gagal, divergensi memori-vs-localStorage tidak pernah terlihat pengguna, dan muat ulang berikutnya memulihkan keadaan yang konsisten. Menambahkan rollback berarti membongkar jalur tulis bersama demi kasus yang tidak dapat diamati. — Bila keliru: bila pengguna menavigasi manual setelah kegagalan, tabel dibangun ulang dari localStorage sehingga record yang gagal memang tidak muncul — konsisten, bukan menyesatkan.

## 23. rentang re-review dimulai dari fbbac21, bukan dari 82fca3e yang dilihat review sebelumnya. — Di antara keduanya ada commit perbaikan Task 12 yang sudah di-review terpisah dan sudah dinyatakan PASS; memasukkannya lagi hanya akan membuat re-reviewer menilai ulang pekerjaan yang sudah lolos gerbangnya sendiri. Rentang fbbac21..8a1a0a5 berisi murni perbaikan Task 13. — Bila keliru: re-reviewer melihat lebih sedikit konteks daripada yang tersedia; risikonya nol karena temuan yang sedang diverifikasi seluruhnya ada di dalam rentang yang lebih sempit itu.

## 24. Task 14 memakai gpt-5.6-sol/low, bukan gpt-6-astra/medium seperti pemetaan awal. — Brief Task 14 memuat kode lengkap untuk ketiga berkas, sehingga pekerjaannya transkripsi teliti plus verifikasi, bukan integrasi yang menuntut penalaran; pemetaan awal dibuat sebelum brief-nya diekstrak dan dilihat isinya. — Bila keliru: satu putaran perbaikan tambahan pada task yang kodenya sudah tertulis lengkap di brief. Task 14: dispatch menekankan tiga hal yang tidak diketahui brief — blok `@media print` wajib menjadi yang PALING AKHIR di style.css (Task 14 adalah penulis terakhir berkas itu), pola `if (!Layout.init({...})) return;`, dan kewajiban `UI.escapeHtml` pada nilai data yang dirender sebagai HTML termasuk angka dan id di dalam atribut, karena contoh di brief membiarkannya mentah dan itu sudah dua kali menjadi temuan reviewer. Task 14 berjalan paralel dengan re-review Task 13: yang satu implementer, yang lain reviewer read-only, dan berkasnya tidak beririsan sama sekali.

## 25. panel saring TIDAK diberi kelas `.toolbar`, melainkan kelas semantik baru yang ditambahkan ke daftar penyembunyian blok cetak. — `.toolbar` membawa gaya layar sungguhan (`display:flex`, `padding:16px`, `border-bottom`) di style.css:412; menempelkannya pada section yang memakai tata letak grid Tailwind akan mengubah tampilan di layar demi memperbaiki tampilan di kertas. — Bila keliru: satu selektor tambahan di blok cetak yang tidak cocok dengan apa pun; nol dampak pada layar.

## 26. diperbaiki dengan menambah setter `setPerHalaman` yang bersifat aditif pada `DataTable`, lalu halaman memperluas tabel ke seluruh baris tersaring pada event `beforeprint` dan memulihkannya pada `afterprint`. — Menyangkutkannya pada event cetak, bukan pada handler tombol Cetak, karena langkah verifikasi brief sendiri memakai Ctrl+P; perbaikan yang hanya memasang diri pada tombol akan meninggalkan jalur Ctrl+P tetap tercetak 10 baris. `terlihat` di table.js:72 sudah berisi seluruh baris tersaring sebelum dipotong, sehingga `barisTampil()` memberi jumlah yang tepat tanpa menghitung ulang penyaringan. Setter-nya aditif sehingga empat halaman lain yang memakai DataTable tidak terpengaruh. — Bila keliru: satu metode publik yang tidak dipanggil siapa pun selain halaman laporan.

## 27. Task 16 memakai gpt-6-astra/medium sesuai pemetaan awal, tidak diturunkan seperti Task 14. — Berbeda dengan Task 14 yang briefnya memuat kode lengkap, brief Task 16 meminta prosa yang harus mencerminkan keadaan kode yang SUDAH BERUBAH sejak rencana ditulis (Pengaturan menjadi modal bukan halaman, bertambahnya laporan.html, bertambahnya dua harness tes), sehingga dibutuhkan penilaian bukan transkripsi. — Bila keliru: biaya token lebih tinggi pada satu task. Task 16: dispatch membawa daftar keadaan kode terkini yang wajib tercermin di dokumentasi, dan melarang tegas mengarang tautan Figma/Stitch/GitHub/Vercel — slotnya harus ditulis sebagai penanda yang mudah dicari, dengan bukti berupa daftar seluruh URL di kedua berkas beserta nomor barisnya agar saya dapat memeriksanya satu per satu.

## 28. temuan tetap DIPERBAIKI meskipun tidak terjangkau, dengan penjaga re-entry satu baris, bukan dengan mengubah kontrak `UI.modal`. — Keamanannya saat ini bersandar sepenuhnya pada dua komponen lain (z-index scrim dan focus trap); keduanya milik task lain dan dapat berubah tanpa siapa pun teringat bahwa modal Pengaturan diam-diam bergantung padanya, sementara mode gagalnya adalah modal dengan tombol mati yang membingungkan. Biaya perbaikannya satu baris, jauh lebih murah daripada menanggung ketergantungan tersembunyi itu sampai Task 17. Penjaga `if (document.getElementById('aturTema')) return;` hanya mencegah modal Pengaturan KEDUA dan tidak menyentuh modal lain, berbeda dengan penjaga umum "ada modal terbuka" yang akan ikut memblokir Pengaturan saat modal konfirmasi lain sedang tampil. — Bila keliru: satu penjaga yang tidak pernah menyala karena jalurnya memang tidak terjangkau.

## 29. temuan 1 (kelas tipografi Tailwind `text-lg`/`text-sm` di `laporan.html`) DITUNDA ke Task 17, bukan diperbaiki di Task 14. — Saya hitung sendiri sebarannya: pola yang sama sudah ada di `dashboard.html` (8 kemunculan), `index.html` (3), dan `pages/layout.html` (1) — ketiganya keluaran Task 9, Task 8, dan Task 7 yang SELURUHNYA lolos review bersih, dan `layout.html` adalah deliverable Milestone 2 yang sudah dikumpulkan. Memperbaiki hanya `laporan.html` justru membuat basis kode LEBIH tidak konsisten, sementara memperbaiki keempatnya adalah perubahan lintas halaman yang memang menjadi lingkup Task 17 ("verifikasi menyeluruh dan perbaikan lintas halaman"). Temuannya sendiri sah — batasan globalnya nyata dan kerapian kelas CSS adalah kriteria penilaian tugas. — Bila keliru: satu kriteria penilaian kerapian CSS baru dibereskan di task terakhir alih-alih sekarang; nol dampak fungsional.

## 30. temuan 2 (kop laporan memakai `formatTanggalPanjang`, bukan `DD Mmm YYYY`) DITOLAK. — Batasan global mengatur bagaimana TANGGAL DATA ditampilkan, dan tabel Rincian Transaksi memang mematuhinya: saya lihat sendiri "14 Sep 2026" di sel-selnya. Yang dipersoalkan reviewer adalah baris prosa pada kop laporan cetak, dan `UI.formatTanggalPanjang` disediakan oleh lapisan UI bersama sejak Task 5 justru untuk keperluan semacam itu. "Periode 17 Juni 2026 sampai 15 September 2026" adalah tipografi yang benar untuk kop laporan resmi; memaksakan "17 Jun 2026" di sana akan memperburuk keluaran demi kepatuhan harfiah. — Bila keliru: satu baris kop memakai format tanggal panjang alih-alih pendek.

## 31. temuan 3 (warna garis bantu grafik ditulis literal, bukan token tema) DITOLAK. — `page-dashboard.js:19-20` memakai pola yang PERSIS SAMA (`rgba(255,255,255,.10)` untuk gelap, `rgba(15,23,42,.10)` untuk terang, dipilih dari kelas `dark`), dan Task 9 lolos review bersih dengan pola itu. Kanvas Chart.js tidak dapat mengonsumsi `var()` CSS sama sekali, sehingga nilai literal yang dipilih berdasarkan tema ADALAH cara yang tersedia; kodenya tetap sadar tema, hanya tidak lewat token. Menolak pola ini di Task 14 saja berarti menghukum konsistensi. — Bila keliru: dua nilai RGBA yang tetap sadar tema tidak ditarik dari token.

## 32. temuan 4 (teks grafik tetap memakai warna layar saat dicetak) DITERIMA, ini temuan yang sungguh-sungguh. — Saya ukur sendiri di peramban lewat `Chart.getChart()`: warna legenda, tick sumbu X, dan tick sumbu Y ketiganya `#94A3B8` (slate-400) yang dipanggang ke dalam kanvas saat render, dan garis grid `rgba(255,255,255,.10)` yang praktis putih. Di atas kertas putih, label sumbu nyaris tak terbaca dan garis grid hilang sama sekali. CSS cetak TIDAK dapat menjangkaunya karena kanvas sudah digambar. Ini kelas cacat yang sama persis dengan label ringkasan yang saya temukan sebelumnya, hanya di permukaan yang tidak bisa disentuh CSS — dan halaman ini seluruh gunanya adalah laporan cetak. — Bila keliru: grafik dicetak dengan warna teks gelap yang sebenarnya sudah terbaca.

## 33. perbaikan Task 14 (warna grafik saat cetak) dan Task 15 (penjaga modal ganda) digabung ke SATU dispatch Codex, meski keduanya milik fix loop task yang berbeda. — Runtime Codex sesi ini hanya menjalankan satu job pada satu waktu, sehingga dua dispatch berarti dua putaran penuh berurutan untuk perubahan yang masing-masing kecil dan sudah saya tentukan bentuknya secara presisi. Berkasnya tidak beririsan (`page-laporan.js` dan `page-pengaturan.js`), jadi keduanya tetap dapat di-commit terpisah dan direview terpisah. — Bila keliru: satu laporan implementer memuat dua topik sekaligus, yang sedikit mengaburkan pembukuan tetapi tidak mengubah kode maupun cakupan review.

## 34. Step 2 brief Task 17 menuliskan ekspektasi "57 / 57 lulus", padahal suite sekarang berisi 60 tes. Angka rencana yang sudah usang, BUKAN kegagalan implementasi. — Suite tumbuh dari 57 menjadi 60 setelah task-task berikutnya menambahkan tes; ledger Task 6 sudah mencatat kumulatif 57 pada saat itu, dan Task 3-6 menambahkan sisanya. Ekspektasi yang benar untuk verifikasi akhir adalah 60/60 ditambah harness sirkulasi 6/6 dan harness form buku 5/5. — Bila keliru: langkah verifikasi mengejar angka yang salah dan saya akan mengira ada tiga tes yang hilang.

## 35. Minor reviewer soal kebutuhan akses internet di README IKUT DIPERBAIKI, meski Minor biasanya ditunda. — Aplikasi ini memuat Tailwind, Chart.js, dan Google Fonts dari CDN, sehingga tanpa internet halaman ter-render tanpa gaya sama sekali; seseorang yang mengikuti README secara offline akan menyimpulkan proyeknya rusak, bukan bahwa ia kurang satu syarat. Biayanya satu baris pada berkas yang memang sedang dibuka untuk perbaikan lain. — Bila keliru: satu baris prasyarat tambahan di README yang tidak diperlukan siapa pun.

## 36. `.btn-primary` memakai `#4F46E5`, yang SUDAH terdefinisi sebagai `brand.600` di `config.js`, bukan warna baru yang dikarang. — Rasionya naik dari 4,47 ke 6,29, dan karena nilainya sudah menjadi bagian palet, hierarki visual serta hubungan dengan warna brand lain tetap utuh; token `brand` DEFAULT tidak diubah sehingga nav aktif dan seri grafik tidak ikut bergeser. — Bila keliru: tombol primer satu langkah lebih pekat dari rancangan semula.

## 37. badge mode terang diperdalam warnanya, bukan permukaannya yang dipekatkan seperti saran Step 7. — Saran brief ditulis dengan asumsi permukaan gelap, tempat memekatkan glass menambah kontras; di mode terang teksnya gelap di atas tint terang, sehingga memekatkan tint justru MENURUNKAN kontras. Larangan brief berbunyi "jangan menerangkan warna teksnya", dan memperdalam warna adalah lawannya, bukan pelanggarannya — hue semantiknya tetap sama, hanya lebih pekat. — Bila keliru: dua warna badge di mode terang satu langkah lebih gelap.

## 38. kehilangan fokus ini DIPERBAIKI, dan diperbaiki di `render()` secara umum, bukan ditambal khusus di jalur papan ketik `<th>`. — Membuat kepala kolom dapat difokus lalu melemparkan fokus ke puncak dokumen setiap kali dipakai berarti pekerjaan Step 8 hanya setengah jadi; pengguna papan ketik harus mengulang Tab dari awal untuk mengurutkan sekali lagi atau berpindah halaman, yang secara praktis membuat kontrol itu tetap tidak terpakai. Menambalnya hanya di jalur `<th>` akan meninggalkan paginasi tetap rusak padahal sebabnya identik, dan pengguna tetikus tidak terpengaruh sama sekali karena pemulihan fokus hanya berlaku bila fokus memang sedang berada di dalam tabel. — Bila keliru: satu blok kecil penyimpan-dan-pulihkan fokus di `render()` yang tidak pernah menyala bagi pengguna tetikus.
